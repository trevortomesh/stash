# Rules.json Guide

`rules.json` stores sorting rules for file extensions and MIME types.

Example:
```json
{
    "extensions": {
        "png": "Images",
        "pdf": "Documents"
    },
    "mime_types": {
        "application/pdf": "Documents"
    }
}
```
